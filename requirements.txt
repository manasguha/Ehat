# Dashboard / deployment dependencies.
# These are the only packages the Streamlit app and the ML pipeline modules it
# imports need, so this file stays light enough for a fast cloud build.
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
plotly>=5.15.0
streamlit>=1.28.0
